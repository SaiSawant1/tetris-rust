use super::engine::Engine;
pub struct Interface {
    engine: Engine,
}

impl Interface {
    pub fn run(_engine: Engine) {
        //TODO: RUN GAME
        // Let interface = Self { engine };
    }
}
